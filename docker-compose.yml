version: "3.9"

x-service-defaults: &service-defaults
  restart: unless-stopped
  networks:
    - erp_net
  env_file:
    - .env

x-backend-healthcheck: &backend-healthcheck
  healthcheck:
    test: ["CMD-SHELL", "wget -qO- http://localhost:$${PORT}/health || exit 1"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 30s

services:
  # ═══════════════════════════════════════════════════════════════════════════
  # INFRASTRUCTURE
  # ═══════════════════════════════════════════════════════════════════════════

  postgres:
    image: postgres:16-alpine
    container_name: erp_postgres
    <<: *service-defaults
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-school_erp}
      POSTGRES_USER: ${POSTGRES_USER:-school_erp}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in your .env file}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-school_erp}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: erp_redis
    <<: *service-defaults
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: erp_mqtt
    <<: *service-defaults
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./infrastructure/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log

  influxdb:
    image: influxdb:2.7-alpine
    container_name: erp_influxdb
    <<: *service-defaults
    ports:
      - "8086:8086"
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUXDB_ADMIN_USER:?INFLUXDB_ADMIN_USER is required}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUXDB_ADMIN_PASSWORD:?INFLUXDB_ADMIN_PASSWORD is required}
      DOCKER_INFLUXDB_INIT_ORG: school-erp
      DOCKER_INFLUXDB_INIT_BUCKET: gps_logs
    volumes:
      - influxdb_data:/var/lib/influxdb2

  # DB migrations (runs once then exits)
  migrate:
    build:
      context: .
      dockerfile: infrastructure/docker/migrate.Dockerfile
    container_name: erp_migrate
    <<: *service-defaults
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    restart: "no"

  # ═══════════════════════════════════════════════════════════════════════════
  # BACKEND MICROSERVICES
  # ═══════════════════════════════════════════════════════════════════════════

  auth-service:
    build:
      context: .
      dockerfile: apps/auth-service/Dockerfile
    container_name: erp_auth
    <<: *service-defaults
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      REDIS_URL: redis://:${REDIS_PASSWORD:-}@redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  user-service:
    build:
      context: .
      dockerfile: apps/user-service/Dockerfile
    container_name: erp_user
    <<: *service-defaults
    ports:
      - "3002:3002"
    environment:
      PORT: 3002
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  student-service:
    build:
      context: .
      dockerfile: apps/student-service/Dockerfile
    container_name: erp_student
    <<: *service-defaults
    ports:
      - "3003:3003"
    environment:
      PORT: 3003
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  academic-service:
    build:
      context: .
      dockerfile: apps/academic-service/Dockerfile
    container_name: erp_academic
    <<: *service-defaults
    ports:
      - "3004:3004"
    environment:
      PORT: 3004
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  attendance-service:
    build:
      context: .
      dockerfile: apps/attendance-service/Dockerfile
    container_name: erp_attendance
    <<: *service-defaults
    ports:
      - "3005:3005"
    environment:
      PORT: 3005
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  fee-service:
    build:
      context: .
      dockerfile: apps/fee-service/Dockerfile
    container_name: erp_fee
    <<: *service-defaults
    ports:
      - "3006:3006"
    environment:
      PORT: 3006
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  notification-service:
    build:
      context: .
      dockerfile: apps/notification-service/Dockerfile
    container_name: erp_notification
    <<: *service-defaults
    ports:
      - "3007:3007"
    environment:
      PORT: 3007
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      REDIS_URL: redis://:${REDIS_PASSWORD:-}@redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  exam-service:
    build:
      context: .
      dockerfile: apps/exam-service/Dockerfile
    container_name: erp_exam
    <<: *service-defaults
    ports:
      - "3008:3008"
    environment:
      PORT: 3008
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  lms-service:
    build:
      context: .
      dockerfile: apps/lms-service/Dockerfile
    container_name: erp_lms
    <<: *service-defaults
    ports:
      - "3009:3009"
    environment:
      PORT: 3009
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  hr-service:
    build:
      context: .
      dockerfile: apps/hr-service/Dockerfile
    container_name: erp_hr
    <<: *service-defaults
    ports:
      - "3010:3010"
    environment:
      PORT: 3010
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  payroll-service:
    build:
      context: .
      dockerfile: apps/payroll-service/Dockerfile
    container_name: erp_payroll
    <<: *service-defaults
    ports:
      - "3011:3011"
    environment:
      PORT: 3011
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  certificate-service:
    build:
      context: .
      dockerfile: apps/certificate-service/Dockerfile
    container_name: erp_certificate
    <<: *service-defaults
    ports:
      - "3012:3012"
    environment:
      PORT: 3012
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      CERTIFICATE_SERVICE_PORT: 3012
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  admission-service:
    build:
      context: .
      dockerfile: apps/admission-service/Dockerfile
    container_name: erp_admission
    <<: *service-defaults
    ports:
      - "3013:3013"
    environment:
      PORT: 3013
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      ADMISSION_SERVICE_PORT: 3013
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  transport-service:
    build:
      context: .
      dockerfile: apps/transport-service/Dockerfile
    container_name: erp_transport
    <<: *service-defaults
    ports:
      - "3014:3014"
    environment:
      PORT: 3014
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      TRANSPORT_SERVICE_PORT: 3014
      MQTT_BROKER_URL: mqtt://mosquitto:1883
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      mosquitto:
        condition: service_started

  health-service:
    build:
      context: .
      dockerfile: apps/health-service/Dockerfile
    container_name: erp_health
    <<: *service-defaults
    ports:
      - "3015:3015"
    environment:
      PORT: 3015
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      HEALTH_SERVICE_PORT: 3015
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  library-service:
    build:
      context: .
      dockerfile: apps/library-service/Dockerfile
    container_name: erp_library
    <<: *service-defaults
    ports:
      - "3016:3016"
    environment:
      PORT: 3016
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      LIBRARY_SERVICE_PORT: 3016
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  ops-service:
    build:
      context: .
      dockerfile: apps/ops-service/Dockerfile
    container_name: erp_ops
    <<: *service-defaults
    ports:
      - "3020:3020"
    environment:
      PORT: 3020
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      OPS_SERVICE_PORT: 3020
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  scholarship-service:
    build:
      context: .
      dockerfile: apps/scholarship-service/Dockerfile
    container_name: erp_scholarship
    <<: *service-defaults
    ports:
      - "3019:3019"
    environment:
      PORT: 3019
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      SCHOLARSHIP_SERVICE_PORT: 3019
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  expense-service:
    build:
      context: .
      dockerfile: apps/expense-service/Dockerfile
    container_name: erp_expense
    <<: *service-defaults
    ports:
      - "3018:3018"
    environment:
      PORT: 3018
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      EXPENSE_SERVICE_PORT: 3018
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  event-service:
    build:
      context: .
      dockerfile: apps/event-service/Dockerfile
    container_name: erp_event
    <<: *service-defaults
    ports:
      - "3017:3017"
    environment:
      PORT: 3017
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      EVENT_SERVICE_PORT: 3017
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  report-service:
    build:
      context: .
      dockerfile: apps/report-service/Dockerfile
    container_name: erp_report
    <<: *service-defaults
    ports:
      - "3021:3021"
    environment:
      PORT: 3021
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      REPORT_SERVICE_PORT: 3021
      CHROMIUM_PATH: /usr/bin/chromium-browser
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  saas-service:
    build:
      context: .
      dockerfile: apps/saas-service/Dockerfile
    container_name: erp_saas
    <<: *service-defaults
    ports:
      - "3022:3022"
    environment:
      PORT: 3022
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      SAAS_SERVICE_PORT: 3022
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:?JWT_ACCESS_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  # ═══════════════════════════════════════════════════════════════════════════
  # API GATEWAY
  # ═══════════════════════════════════════════════════════════════════════════

  api-gateway:
    build:
      context: .
      dockerfile: apps/api-gateway/Dockerfile
    container_name: erp_api_gateway
    <<: *service-defaults
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:4000,http://localhost:4001,http://localhost:4002,http://localhost:4003,http://localhost:4004}
      AUTH_SERVICE_URL: http://auth-service:3001
      USER_SERVICE_URL: http://user-service:3002
      STUDENT_SERVICE_URL: http://student-service:3003
      ACADEMIC_SERVICE_URL: http://academic-service:3004
      ATTENDANCE_SERVICE_URL: http://attendance-service:3005
      FEE_SERVICE_URL: http://fee-service:3006
      NOTIFICATION_SERVICE_URL: http://notification-service:3007
      EXAM_SERVICE_URL: http://exam-service:3008
      LMS_SERVICE_URL: http://lms-service:3009
      HR_SERVICE_URL: http://hr-service:3010
      PAYROLL_SERVICE_URL: http://payroll-service:3011
      CERTIFICATE_SERVICE_URL: http://certificate-service:3012
      ADMISSION_SERVICE_URL: http://admission-service:3013
      TRANSPORT_SERVICE_URL: http://transport-service:3014
      HEALTH_SERVICE_URL: http://health-service:3015
      LIBRARY_SERVICE_URL: http://library-service:3016
      EVENT_SERVICE_URL: http://event-service:3017
      EXPENSE_SERVICE_URL: http://expense-service:3018
      SCHOLARSHIP_SERVICE_URL: http://scholarship-service:3019
      OPS_SERVICE_URL: http://ops-service:3020
      REPORT_SERVICE_URL: http://report-service:3021
      SAAS_SERVICE_URL: http://saas-service:3022
      AI_SERVICE_URL: http://ai-service:8000
      BIOMETRIC_SERVICE_URL: http://biometric-bridge:8080
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ═══════════════════════════════════════════════════════════════════════════
  # FRONTEND PORTALS
  # ═══════════════════════════════════════════════════════════════════════════

  admin-portal:
    build:
      context: .
      dockerfile: apps/admin-portal/Dockerfile
    container_name: erp_admin_portal
    <<: *service-defaults
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      NEXT_PUBLIC_API_URL: http://api-gateway:3000
    depends_on:
      api-gateway:
        condition: service_healthy

  teacher-portal:
    build:
      context: .
      dockerfile: apps/teacher-portal/Dockerfile
    container_name: erp_teacher_portal
    <<: *service-defaults
    ports:
      - "4001:4001"
    environment:
      PORT: 4001
      NEXT_PUBLIC_API_URL: http://api-gateway:3000
    depends_on:
      api-gateway:
        condition: service_healthy

  student-portal:
    build:
      context: .
      dockerfile: apps/student-portal/Dockerfile
    container_name: erp_student_portal
    <<: *service-defaults
    ports:
      - "4002:4002"
    environment:
      PORT: 4002
      NEXT_PUBLIC_API_URL: http://api-gateway:3000
    depends_on:
      api-gateway:
        condition: service_healthy

  parent-portal:
    build:
      context: .
      dockerfile: apps/parent-portal/Dockerfile
    container_name: erp_parent_portal
    <<: *service-defaults
    ports:
      - "4003:4003"
    environment:
      PORT: 4003
      NEXT_PUBLIC_API_URL: http://api-gateway:3000
    depends_on:
      api-gateway:
        condition: service_healthy

  management-portal:
    build:
      context: .
      dockerfile: apps/management-portal/Dockerfile
    container_name: erp_management_portal
    <<: *service-defaults
    ports:
      - "4004:4004"
    environment:
      PORT: 4004
      NEXT_PUBLIC_API_URL: http://api-gateway:3000
    depends_on:
      api-gateway:
        condition: service_healthy

  developer-api:
    build:
      context: apps/developer-api
      dockerfile: Dockerfile
    container_name: erp_developer_api
    <<: *service-defaults
    ports:
      - "3023:3023"
    environment:
      PORT: 3023
      DEVELOPER_API_PORT: 3023
      DATABASE_URL: postgresql://${POSTGRES_USER:-school_erp}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-school_erp}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:?JWT_ACCESS_SECRET is required}
      WEBHOOK_ENCRYPTION_KEY: ${WEBHOOK_ENCRYPTION_KEY:?WEBHOOK_ENCRYPTION_KEY is required}
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3023/v1/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  biometric-bridge:
    build:
      context: apps/biometric-bridge
      dockerfile: Dockerfile
    container_name: erp_biometric_bridge
    <<: *service-defaults
    ports:
      - "8080:8080"
    environment:
      SCHOOL_ID: ${SCHOOL_ID:-school_demo}
      DEVICES: ${BIOMETRIC_DEVICES:-MainGate:192.168.1.201:4370}
      POLL_INTERVAL_SECONDS: ${BIOMETRIC_POLL_INTERVAL:-60}
      FETCH_WINDOW_MINUTES: "10"
      LOCAL_MQTT_URL: mqtt://mosquitto:1883
      CLOUD_MQTT_URL: mqtt://mosquitto:1883
      ATTENDANCE_SERVICE_URL: http://attendance-service:3005
      NOTIFICATION_SERVICE_URL: http://notification-service:3007
      HEALTH_ALERT_THRESHOLD_MINUTES: "30"
    depends_on:
      mosquitto:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      start_period: 20s
      retries: 3

networks:
  erp_net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  mosquitto_data:
  mosquitto_log:
  influxdb_data:
