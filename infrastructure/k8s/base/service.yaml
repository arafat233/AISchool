apiVersion: v1
kind: Service
metadata:
  name: SERVICE_NAME
  namespace: school-erp
  labels:
    app: SERVICE_NAME
spec:
  selector:
    app: SERVICE_NAME
  ports:
    - name: http
      port: SERVICE_PORT
      targetPort: SERVICE_PORT
  type: ClusterIP
